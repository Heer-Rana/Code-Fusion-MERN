/**
 * @typedef {Object} ICopilotContext
 * @property {(input: string) => void} setInput
 * @property {string} output
 * @property {boolean} isRunning
 * @property {() => void} generateCode
 */

// IMPORTANT: Add this line below your JSDoc definition.
// This creates an empty JavaScript object that can be imported at runtime.
// Your JSDoc @typedef for ICopilotContext will still provide type hints for this object.
export const ICopilotContext = {};

// If you have other typedefs in this file that you are trying to import,
// make sure to add similar `export const MyOtherType = {};` lines for them as well.